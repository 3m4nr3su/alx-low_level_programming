#include <stdio.h>
#include "main.h"

/**
 *main - Entry point
 *
 *Return: Always 0 (Success)
 */

int positive_or_negative(int n)
{
	n = 0;

	if (n == 0)
		printf("%d is zero\n", n);

	return (0);
}
