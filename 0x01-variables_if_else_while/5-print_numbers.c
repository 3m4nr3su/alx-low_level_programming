#include <stdio.h>

/**
 * main - count single digit no.s
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int no = 0;

	while (no < 10)
	{
		printf("%i", no);
		no++;
	}
	printf("\n");
	return (0);
}
